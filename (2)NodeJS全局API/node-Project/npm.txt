1.初始化

2.按提示输入，敲回车

3.初始化完成后会有一个package.json

4.可以使用npm install 模块名 --save-dev
就可以将安装模块的信息存入到package.json文件中

5.nrm 用来管理第三方模块的下载镜像
安装nrm,  npm install nrm -g
nrm ls 来查看所有的镜像
nrm use镜像名字  来切换下载服务器

当原来镜像不存在某个模块，或者下载速度很慢时
可以尝试切换下载服务器。